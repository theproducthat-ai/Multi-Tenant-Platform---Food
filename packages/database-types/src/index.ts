/**
 * Generated Supabase database types. Regenerate with:
 *   npm run generate --workspace=@platform/database-types
 * (wraps `supabase gen types typescript --linked`, requires the CLI to be
 * authenticated and linked to the target project — see docs/DEVELOPMENT_SETUP.md).
 *
 * No business tables exist yet (Module 0A) — the generated `Database` type
 * below only reflects Supabase's built-in schemas.
 */
export type { Database, Json, Tables, TablesInsert, TablesUpdate, Enums, CompositeTypes } from './database';
export { Constants } from './database';
